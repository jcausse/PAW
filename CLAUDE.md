# Claude Agentic Coding Rules

This file is needed because Claude models do not read `AGENTS.md` by default.

All agents must:
- Read file `AGENTS.md` to find all rules centralized.
- List directory `.agents` to find resources meant for AI agents.
- List directory `.agents/skills` to find AI agent skills.
